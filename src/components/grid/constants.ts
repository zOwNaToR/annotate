import { GridNumberToPositionMapType } from '@/components/grid/types';
import { COLUMN_POSITION } from '@/components/annotation-container/constants';

export const mapColNumberToGridPosition: GridNumberToPositionMapType = {
  1: COLUMN_POSITION.LEFT,
  2: COLUMN_POSITION.RIGHT,
};